@{
    ViewBag.Title = "TaxTypeMaster";
    Layout = "~/Views/Shared/_Layout.cshtml";
    
    <link href="~/Content/css/bootstrap.min.css" rel="stylesheet" />
    <link href="~/Content/css/FormStyle.css" rel="stylesheet" />

}

<script>
    //$(".activity_repoty_table").tablesorter();
    // A $( document ).ready() block.
    $(document).ready(function () {
        $("#EditColorPartial").hide();

    });
    function Save() {
        if ($('#TaxName').val() == "") {
            alert("Please Enter Tax Name.")
            $('#TaxName').css('border-color', 'red');
            $('#TaxName').focus();
            return false;
        }
        else {
            $('#TaxName').css('border-color', '');
        }
        //if ($('#TaxMode').val() == "") {
        //    alert("Please Enter Tax Mode.")
        //    $('#TaxMode').css('border-color', 'red');
        //    $('#TaxMode').focus();
        //    return false;
        //}
        //else {
        //    $('#TaxMode').css('border-color', '');
        //}
        if ($('#TaxRef').val() == "") {
            alert("Please Select Tax Ref.")
            $('#TaxRef').css('border-color', 'red');
            $('#TaxRef').focus();
            return false;
        }
        else {
            $('#TaxRef').css('border-color', '');
        }
        if ($('#TaxValue').val() == "") {
            alert("Please Enter Tax Value.")
            $('#TaxValue').css('border-color', 'red');
            $('#TaxValue').focus();
            return false;
        }
        else {
            $('#TaxValue').css('border-color', '');
        }
        if ($('#TaxValueMode').val() == "") {
            alert("Please Select Value Mode.")
            $('#TaxValueMode').css('border-color', 'red');
            $('#TaxValueMode').focus();
            return false;
        }
        else {
            $('#TaxValueMode').css('border-color', '');
        }

        if ($('#Form').val() == "") {
            alert("Please Select Form.")
            $('#Form').css('border-color', 'red');
            $('#Form').focus();
            return false;
        }
        else {
            $('#Form').css('border-color', '');
        }

        //if ($('#CostOfTheProduct').val() == "") {
        //    alert("Please Enter Cost Of the Product.")
        //    $('#CostOfTheProduct').css('border-color', 'red');
        //    $('#CostOfTheProduct').focus();
        //    return false;
        //}
        //else {
        //    $('#CostOfTheProduct').css('border-color', '');
        //}
       
        $.ajax({            
            type: 'POST',
            url: '/TaxTypeMaster/TaxTypeMaster',
            data: {
                TaxName: $('#TaxName').val(),
                TaxRef: $('#TaxRef').val(),
                TaxValue: $('#TaxValue').val(),
                TaxValueMode: $('#TaxValueMode').val(),
                Form: $('#Form').val(),
                TaxOn: $('#TaxOn').val(),
                //TaxMode: $('#TaxMode').val(),                
                //TaxCode: $('#TaxCode').val(),           
              
                //CostOfTheProduct: $('#CostOfTheProduct').val(),                
                //AgentMasterId: $('#AgentMasterId').val()
            },
            success: function (data) {
                if (data.TaxMasterID != "" || data.TaxMasterID != 0) {
                    alert('Saved Successfully.');
                    location.href = "/TaxTypeMaster/TaxTypeMaster";
                    return false;
                }
                else if (data.TaxMasterID == 0 && data.TaxName != "") {
                    alert('Already Exist in the database.');
                    return false;
                }
                else {
                    alert('Save process failed');
                    location.href = "/TaxTypeMaster/TaxTypeMaster";
                    return false;
                }

            },
            error: function (ex) {
                alert('Already Exist in the database.');
            }
        });
    }
    function Update(arg) {
        if ($('#TaxName').val() == "") {
            alert("Please Enter Tax Name.")
            $('#TaxName').css('border-color', 'red');
            $('#TaxName').focus();
            return false;
        }
        else {
            $('#TaxName').css('border-color', '');
        }
        //if ($('#TaxMode').val() == "") {
        //    alert("Please Enter Tax Mode.")
        //    $('#TaxMode').css('border-color', 'red');
        //    $('#TaxMode').focus();
        //    return false;
        //}
        //else {
        //    $('#TaxMode').css('border-color', '');
        //}
        if ($('#TaxRef').val() == "") {
            alert("Please Select Tax Ref.")
            $('#TaxRef').css('border-color', 'red');
            $('#TaxRef').focus();
            return false;
        }
        else {
            $('#TaxRef').css('border-color', '');
        }
        if ($('#TaxValue').val() == "") {
            alert("Please Enter Tax Value.")
            $('#TaxValue').css('border-color', 'red');
            $('#TaxValue').focus();
            return false;
        }
        else {
            $('#TaxValue').css('border-color', '');
        }
        if ($('#TaxValueMode').val() == "") {
            alert("Please Select Value Mode.")
            $('#TaxValueMode').css('border-color', 'red');
            $('#TaxValueMode').focus();
            return false;
        }
        else {
            $('#TaxValueMode').css('border-color', '');
        }

        if ($('#Form').val() == "") {
            alert("Please Select Form.")
            $('#Form').css('border-color', 'red');
            $('#Form').focus();
            return false;
        }
        else {
            $('#Form').css('border-color', '');
        }

        //if ($('#CostOfTheProduct').val() == "") {
        //    alert("Please Enter Cost Of the Product.")
        //    $('#CostOfTheProduct').css('border-color', 'red');
        //    $('#CostOfTheProduct').focus();
        //    return false;
        //}
        //else {
        //    $('#CostOfTheProduct').css('border-color', '');
        //}
    
        $.ajax({
            type: 'POST',
            url: '/TaxTypeMaster/Update',
            data: {
                TaxName: $('#TaxName').val(),
                TaxRef: $('#TaxRef').val(),
                TaxValue: $('#TaxValue').val(),
                TaxValueMode: $('#TaxValueMode').val(),
                Form: $('#Form').val(),
                TaxOn: $('#TaxOn').val(),
                //TaxMode: $('#TaxMode').val(),               
                //TaxCode: $('#TaxCode').val(), 
                
                //CostOfTheProduct: $('#CostOfTheProduct').val(),                
                TaxMasterID: $('#TaxMasterID').val()
            },
            success: function (data) {
                if (data.TaxMasterID != "" || data.TaxMasterID != 0) {
                    alert('Updated Successfully.');
                    location.href = "/TaxTypeMaster/TaxTypeMaster";
                    return false;
                }
                else {
                    alert('Updation failed.');
                    location.href = "/TaxTypeMaster/TaxTypeMaster";
                    return false;
                }

            },
            error: function (ex) {
                alert('Already Exist in the database.');
            }
        });
    }
</script>
<h2 class="content-title">Tax Type Master</h2>
<div style="width: 100%; float: left;">

    <div id="tabVehicleWatchList">
        @Html.Action("TaxTypeMasterGrid", "TaxTypeMaster")
    </div>
</div>
