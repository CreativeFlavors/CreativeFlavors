@model MMS.Web.Models.StockModel.OrderEntryModel

@{
    bool Order = true;
    int count = 1;
  
}
<script>
    $.noConflict();
    $(function () {
        $(".hasDatepicker2").datepicker({ dateFormat: "dd/mm/yy" });
        $("#Date").datepicker({ dateFormat: "dd/mm/yy", maxDate: new Date() });
        $("#CustomerDate").datepicker({ dateFormat: "dd/mm/yy", maxDate: new Date() });
    });
</script>
<div>
    <input type="text" id="ExFactoryDate" class="fromDate large hasDatepicker2" maxlength="10" placeholder="dd/mm/yyyy" autocomplete="on">
</div>

